# NH 2nd Caesar Cipher

options = input("Hello and welcome to Code or Decode! Press '1' to encode a phrase and '2' to decode a phrase.")
if options == 1:
    shift_value=input("And what's the shift value?")