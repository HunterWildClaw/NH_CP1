# NH 2nd debugging notes. (I need it...)

#Syntax, Logic, and Run-Time errors.
#Sigghhh. Looks like this isn't what I need.
#Fix by reading where problem is and fixing it. For Syntax
#Fix by working through you code step by step. Rubber-duck debugging. For Logic
#Fix by using the debugger.

import random

while True:
    denominator = random.randint(0,5)

    print(10/denominator)